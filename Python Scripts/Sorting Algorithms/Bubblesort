# As part of a task in a class, I had to implement a sorting algorithm to sort a set of numbers
# While I was already familiar with methods such as quicksort and merge sort, I decided to attempt to implement a version of Bubblesort instead

deck = [5, 7, 1, 9, 2, 8, 12, 11, 4, 3, 10, 6]
n = 0
for i in range(len(deck)-1):
    for n in range(len(deck)-1):
        if deck[n] > deck[n+1]:
            m = deck[n+1]
            deck[n+1] = deck[n]
            deck[n] = m
        else:
            pass
    n+=1
print(deck)
